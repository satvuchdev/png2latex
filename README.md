# png2latex
# Подробное описание программы и использованных методов находится в файле png2latex.pdf
Программный инструмент для автоматического преобразования растровых PNG-изображений в параметрические кривые с экспортом уравнений в LaTeX.

---

## 📌 Описание проекта

Проект разработан в рамках учебно-исследовательской работы. Цель — создание конвейера для автоматической векторизации графических объектов с получением математически строгих параметрических уравнений.

Основная идея:
Преобразовать растровое изображение (PNG) → бинарная маска → контуры → упрощённые точки → параметрические кусочно-кубические сплайны → уравнения в LaTeX.

---

## 🔧 Функционал

- Загрузка PNG-изображений с поддержкой прозрачности (RGBA)
- Пороговая сегментация для выделения белых областей
- Поиск контуров алгоритмом Marching Squares (find_contours)
- Упрощение контуров алгоритмом Рамера–Дугласа–Пекера (RDP)
- Параметризация по длине дуги (нормированный параметр t ∈ [0, 1])
- Аппроксимация PCHIP-сплайнами (монотонная кусочно-кубическая интерполяция)
- Экспорт параметрических уравнений в формате LaTeX
- Визуализация этапов обработки с помощью Matplotlib

---

## ⚙️ Используемые технологии и библиотеки

- Python 3.9+
- numpy
- Pillow (PIL)
- matplotlib
- scikit-image
- scipy
- subprocess

---

## 📂 Структура проекта

png2latex/
- ├── program/
- │   └── images/                    # Изображения для тестирования программы
- │   │   ├── circle.png
- │   │   ├── oval.png
- │   │   ├── rhombus.png
- │   │   ├── ring.png
- │   │   ├── square.png
- │   │   └── triangle.png
- │   ├── main.py                    # ОСНОВНОЙ ФАЙЛ
- ├── curve_equations_latex.txt      # Выходной файл с уравнениями в LaTeX
- ├── README.md
- └── requirements.txt

---

## ▶️ Запуск программы

### 1. Установка зависимостей

Убедитесь, что установлены все зависимости:
``` bash
pip install -r requirements.txt
```

### 2. Подготовка изображений

Поместите PNG-файлы в папку images/. Изображения должны содержать белые объекты на прозрачном/контрастном фоне.


### 3. Запуск программы

``` bash
python program/main.py
```

---

## 📊 Анализ алгоритмов

### 1. RDP-алгоритм

- Назначение: Упрощение контуров
- Сложность: O(n²) в худшем случае, O(n log n) на практике
- Параметр: epsilon = 5 (оптимально для сохранения формы)

### 2. PCHIP-интерполяция

- Назначение: Построение монотонных кусочно-кубических сплайнов
- Преимущества: Гарантирует отсутствие осцилляций и самопересечений
- Сложность: O(k), где k — количество точек после RDP

### 3. Суммарная сложность

- O(n²) в худшем случае (доминирует RDP)
- На практике — O(n log n) для типичных контуров

---

## ✅ Результаты и точность

Высокая точность на гладких контурах (круг, овал)
Хорошее качество на фигурах с малым числом точек (квадрат, треугольник)
Ограничения: Скругление углов и искривление прямых линий на фигурах типа ромба
Гарантируется: Монотонность, отсутствие осцилляций, гладкость

---

## 🚀 Возможные улучшения

1. Адаптивная аппроксимация — выбор метода в зависимости от типа сегмента (прямая/кривая)
2. Графический интерфейс для интерактивной настройки параметров
3. Экспорт в SVG и другие векторные форматы
4. Расширенная сегментация по произвольному цвету (RGB/HSL)
5. Поддержка сплайнов более высокой степени для точности эллипсов

---

## 📄 Авторство

Автор: satvuchdev
